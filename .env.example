# Copy this file to .env.local and fill in your backend URL.
# CRA only embeds vars prefixed with REACT_APP_, and only at build time -
# changing this requires restarting `npm start` (dev) or a rebuild (prod).
REACT_APP_API_URL=http://localhost:8080
